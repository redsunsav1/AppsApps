meta:
  environment: node
  toolchain:
    name: npm
    version: 20.11.0

build:
  requirements: package.json
  execute: |
    npm install
    npm run build

run:
  persistence:
    - mountPath: /data
  containerPort: 3000
  command: npm start